# ToolBox Android 设计规范

> 本文件统一维护宿主设计与组件映射；架构和安全约束见 [技术方案](docs/ToolBox_Android_技术方案.md)。
> 按用户要求已撤销自动截图测试/基线门禁；仅保留 debug 的 IDE 手动预览，不再要求更新 golden。
> 设计系统：Miuix `v0.9.4-rc01` + Haze `1.7.3` + ToolBox 适配层
> 目标：紧凑、可用、内容优先；不以审核、安全状态或宿主装饰占据工具屏幕。

## 1. 设计结论

ToolBox 是安静、紧凑、可信赖的个人小工具架，不是安全控制台。用户应快速完成导入、打开、授权、后台任务和删除；
任何没有真实操作的状态、卡片、角标或按钮都不出现。

- 顶级目的地只有 **工具** 和 **设置**。导入在工具页顶部操作区；没有独立“导入”“审核”或
  “权限中心”Tab。
- 工具运行时由 HTML 内容填满系统安全区域，使用系统返回手势或按键退出；宿主不叠放按钮或工具栏。
- 权限是工具详情的真实开关页，而非安装前的审核步骤或纯文本“同意/拒绝”。
- 设置只放外观、后台总开关、Developer Help 等真实功能；不放审计留存、信任模式、签名密钥、
  诊断占位或“将来开放”行。
- 使用 grouped-list、分组 surface 与紧凑行；不把每一项套成单独大卡片。
- 一级页面使用大标题，二级页面使用紧凑标题栏；运行页无标题栏。视觉层级来自排版、分组和留白。
  Miuix 保持原组件比例；Liquid Glass 只在导航和局部悬浮操作上使用玻璃，内容区保持清晰底板。
- 首页利用已有 `lastOpenedAt` 展示最近使用图标行，可见数量按可用宽度与最小点击区域自动计算，
  超出部分横向滚动；没有记录或正在搜索时隐藏。`lastOpenedAt` 表示目录接受一次打开请求的时间，
  不是网页成功加载/可交互时间；导航不再等待最近使用写入。写入失败提示“最近使用记录未保存”，
  不把它冒充运行失败，用户下次打开可重试。排队中的同工具请求合并；已知删除的排队请求丢弃。
  安装版本、运行资格及安全校验仍由原 runtime 准备/permit 链路决定，统计成功不构成授权。

## 2. 信息架构

```text
工具
├── 正在运行（有后台会话时） / 最近使用 / 已安装列表 / 空状态 / 搜索结果
├── 导入 .tbx
├── 工具详情
│   ├── 打开
│   ├── 权限
│   ├── 后台任务
│   └── 删除
└── 运行容器

设置
├── 外观
│   ├── Miuix / Liquid Glass
│   ├── 跟随系统 / 浅色 / 深色
│   ├── 系统取色
│   └── 降低透明度（仅 Liquid Glass）
├── 后台保障
├── 工具权限
└── Developer Help
    ├── 搜索 / 复制完整手册
    ├── 快速开始 / 声明与权限
    ├── 基础接口 / 网络请求
    ├── 后台与通知 / 系统能力
    ├── 打包与排错 / 完整 TypeScript 接口
    └── 安装四个范例
```

没有工具时，工具页显示一段简短说明、`导入 .tbx` 主操作和 `安装四个范例` 次操作；不伪造
默认工具或本机目录。

## 3. 双主题与视觉 Token

业务页面只使用 ToolBox 组件和语义 token，不按主题复制页面。`ThemeStyle` 只决定组件外观；
`ThemeMode` 继续决定明暗与系统取色。主题切换通过 DataStore → SettingsViewModel → 根主题生效，
不得以 key 重建 Activity、导航栈、ViewModel 或 WebView。

### Miuix

| Token | 明亮模式 | 用途 |
|---|---:|---|
| `canvas` | `#F7F7F7` | 状态栏、页面和分组外背景 |
| `surface` | `#FFFFFF` | grouped-list、弹层、菜单 |
| `surfaceMuted` | `#F0F0F2` | 搜索框、禁用/弱选中背景 |
| `primary` | `#1264CC` | 主操作、选中、链接、焦点；白色按钮文字达到 4.5:1 |
| `textPrimary` | `#17181A` | 标题、正文 |
| `textSecondary` | `#676A70` | 描述、元数据；在 canvas/surface/surfaceMuted 上达到 4.5:1 |
| `separator` | `#E7E7EA` | 分组内分隔线 |
| `danger` | `#BA252C` | 删除等破坏性操作；搭配独立的 `onDanger` |
| `warning` | `#FF9500` | 实际可恢复失败提示 |

使用 Android 系统字体。颜色必须经 ToolBox 语义化主题暴露，业务 Composable 不散落十六进制
颜色。深色画布为 `#111214`，主要 surface 为 `#1C1D20`，弱 surface 为 `#2C2D31`；不打包
Apple/小米专有字体或素材。暗色蓝色按钮使用深色 `onPrimary`，危险按钮不复用主按钮文字色；
Monet 对主/危险按钮文字执行对比度检查，低于 4.5:1 时采用更可读的黑/白前景。禁用态另保留弱化反馈。

### Liquid Glass

| Token | 浅色 | 深色 | 用途 |
|---|---:|---:|---|
| `canvas` | `#F2F2F7` | `#000000` | 页面背景 |
| `surface` | `#FFFFFF` | `#1C1C1E` | 列表、正文、输入和弹层正文 |
| `surfaceMuted` | `#E9E9EE` | `#2C2C2E` | 搜索、弱选中和状态区 |
| `textPrimary` | `#1C1C1E` | `#F5F5F7` | 标题、正文 |
| `textSecondary` | `#5F5F64` | `#AEAEB2` | 说明和元数据；在三种内容底板上达到 4.5:1 |
| `primary` | 系统强调色或 ToolBox 蓝 | 同左 | 主操作、选中、链接；不染内容底板 |
| `glassTint` | `surface / 72%` | `surface / 66%` | 原生导航与局部悬浮操作 |

Liquid Glass 一级标题为 34sp，正文 17sp，辅助文字 13sp；分组圆角 20dp、卡片 22dp、
控件 16dp。Haze 模糊半径 24dp，只采集原生内容层。工具与设置共用一套常驻一级外壳和采集状态，
切页只替换内容，不重新创建顶栏、底栏或背景源。顶栏材质在下缘渐隐；底栏选中项使用不嵌套模糊的
高光透镜，在两个等宽区域之间移动，按压反馈只缩放内容而不改变命中区和布局。
降低透明度、硬件加速不可用、页面被覆盖或转场未完成时停止采集并使用同色不透明材质。

### 尺寸与密度

| 元素 | 合同 |
|---|---|
| 页面水平边距 | 16dp（中宽布局可到 24–28dp） |
| 内容分组间距 | 20–24dp |
| 分组圆角 | 16–18dp |
| 搜索框 | 48dp |
| 工具行 | 72–80dp；字体变大时自然增高 |
| 设置/权限行 | 64–72dp |
| 图标/文字间距 | 12dp |
| 顶部工具栏内容区 | 约 48–52dp，不含状态栏 |
| 主导航视觉区 | 约 56dp；系统导航/手势区域单独且只消费一次 |
| 最小命中目标 | 48dp |

禁止通过 `fontScale` 乘整个容器。文字使用 `sp` 自然缩放，布局以换行和自然高度适配。

## 4. 系统栏、滚动和动效

- 启动依赖加载/错误页跟随系统浅深主题；首个业务页面等待 DataStore 外观解析完成后再显示。
  新安装默认 Liquid Glass + 跟随系统；存在旧设置或宿主数据库且没有风格字段时一次性补齐为 Miuix。
- 系统栏按当前窗口与实际图标明暗作为 effect key，避免同一组合位置的无关重组重复配置；
  新窗口或重新进入 Loading/Error/Ready 组合时仍初始化，不建立跨 Activity 的全局状态。
- 状态栏/cutout 由顶栏所在 surface 消费；导航/手势 inset 由底部导航 surface 消费；IME 只由
  当前输入内容消费。任何 inset 不得在父/子层重复追加。
- 主导航透明贴合系统导航区域，视觉上只保留系统自身手势小白条；不要绘制第二条“沉浸式白条”。
- 运行容器没有 ToolBox 顶栏或底栏，WebView 填满状态栏、导航栏与挖孔之外的安全区域；IME 由工具页面/输入框处理。
- 每个方向只有一个 `Lazy` 滚动容器；工具列表使用 stable key 与 content type；不要在行内使用
  重阴影、复杂 clip、同步图标读取或嵌套滚动。
- 一级 Tab 不对整张页面做透明度、位移或缩放转场，避免白色顶栏在切换中露出灰色画布；动态反馈只作用于
  底栏选中项。二级页面采用常驻分层：底页保持已组合、已测量且静止；目标页完成组合、
  测量并给出稳定首帧信号后，只让最上层
  页面沿进入方向滑动；返回时顶页反向离场，不得同时改变前后两页的透明度、位移或尺寸。
- runtime 完全呈现并遮住底页后，工具首页的目录/导入/运行中展示可暂停 UI 收集；入场与来源页返回前
  恢复并读取当前权威状态。底页组合、列表位置与 VM/仓库/后台会话保留；不暂停导航或权限副作用。
  已打开的卸载/停止弹层继续接收状态，以便目标失效时关闭，不冻结危险确认。
- 运行页先立即滑入轻量原生页面壳；页面壳转场结束后才创建 WebView，首帧完成后原地揭示内容。
  WebView 本体不得参与 alpha、scale、translation、共享元素或整页转场，也不得与可见转场并发初始化。
- 转场只读取 `graphicsLayer` 阶段的动画值，不得让动画进度驱动业务 Composable 每帧重组；页面
  栈变更、数据库读取、文件准备和 WebView 创建不得与可见转场挤在同一帧。
- 转场使用同一套 easing，时长 160–180ms，不叠加整页缩放、弹簧或动态模糊。系统关闭动画时
  禁用非必要动效。

## 5. 页面规范

### 工具页

```text
┌──────────────────────────────┐
│ 工具                    导入  │
│ ┌──────────────────────────┐ │
│ │ 🔍 搜索工具              │ │
│ └──────────────────────────┘ │
│ 正在运行                  2 个│
│ ┌──────────────────────────┐ │
│ │ GitHub 构建守望 [● 运行中]│ │
│ │ 通知实验室      [● 运行中]│ │
│ └──────────────────────────┘ │
│ 最近使用                     │
│ [图标] [图标] [图标] …      │
│ 全部工具 · 4                 │
│ ┌──────────────────────────┐ │
│ │ [图标] 仓位计算器    管理 │ │
│ ├──────────────────────────┤ │
│ │ [图标] 快速笔记      管理 │ │
│ └──────────────────────────┘ │
├──────────────────────────────┤
│          工具        设置     │
└──────────────────────────────┘
```

- 标题右侧为可点击导入按钮，命中区至少 48dp；Liquid Glass 使用圆形加号，Miuix 保留文字操作。
  标题不重复显示工具数量或占位副标题。
- 数量只在“全部工具 · N / 搜索结果 · N”分组标题展示；无匹配结果明确为 0。最近使用只显示
  图标，名称用于无障碍描述；列表以名称为主，版本/大小保留在详情的信息区。
- 名称自然换行、行高随大字增长；不改图标尺寸、不叠加行卡片或固定更矮的行高。
- 搜索只做工具名/ID/分类过滤；不把签名、风险、来源或“本机目录”当列表元信息。
- “正在运行”放在搜索与反馈下方、“最近使用”上方；仅使用现有后台会话投影，没有会话时整个分组
  隐藏。每个会话独立一行，名称/图标打开工具，绿色“运行中”按钮确认后停止该会话；不影响其他工具。
  停止期间显示“停止中”，完成后由真实会话状态移除该行；失败显示可重试反馈。搜索不隐藏运行区。
- 名称与停止按钮是独立的 TalkBack 操作，命中区至少 48dp；名称支持换行、状态按钮使用浅/深色
  语义色。运行状态只在首页分组收集，通知刷新不驱动首页导航重组。
- 保留后台保障的运行管理入口；本次首页入口复用相同停止逻辑，不改变系统通知划除机制。
- 行主区域打开工具；右侧独立的“管理”文字按钮进入详情，宽高至少 48dp，TalkBack 标注
  “管理[工具名]”。两个操作是兄弟命中区域，管理点击不得同时触发打开；不提供重复操作弹层。
- 删除使用确认弹层；不把删除放在误触的行主操作中。

### 导入反馈

选择文件后使用轻量加载状态。成功回到列表并显示“已安装：工具名”；失败显示一行人能执行的
原因，如“包缺少入口文件”或“文件损坏，请重新选择”。不显示权限预选择、审核分数、签名、
发布者、风险等级、恢复状态或“继续审核”。

首次安装和更高 `versionCode` 直接安装。同版本或更低版本通过全部包检查后显示单一确认弹层，
同时列出当前与待安装的 `version / versionCode`；取消即删除暂存包，确认才继续原子替换。弹层说明
运行环境、后台任务和安全存储会清理，普通 KV 与新 manifest 仍声明的权限选择会保留。安全、签名、
宿主最低版本和必要能力失败仍直接拒绝，不提供绕过确认。

### 工具详情与权限

详情顶部身份块只显示实际图标、名称和“打开工具”主操作。管理区仅保留权限、后台任务；
版本、工具大小和最近打开时间集中在信息区，不在身份块重复。删除放在底部独立危险区域。
详情不显示右上角“更多操作”与重复的“工具操作”弹层。删除入口使用有底色的 Miuix
`ToolBoxDestructiveButton`，点击后仍必须确认，确认按钮保留红底白字。后台保障的停止和后台任务的
停止/取消复用相同弱强调危险按钮：浅深色 `softDanger/onSoftDanger`，至少 48dp，不能退回透明红字链接。

列表、最近使用、首页运行区和详情图标以当前安装版本的 `manifest.icon` 为准，并与通知内容图标、
HyperOS 岛图共享同一份异步解码结果；内置资源/分类图标仅作加载中或无有效图片时的回退。等比缩放、
不染色，纯白透明标记使用统一深色底板。ToolBox 启动图标和系统通知来源标识保持宿主身份。
同工具读取/失效按键串行，跨工具最多两个解码并行；缓存命中不占解码名额，像素判断不构建装箱列表。

权限页按 manifest 声明展示每工具 capability。每一项用整行可点的 Miuix switch setting：标题、
简短说明、开关。没有 handler 的能力不显示为可授予；系统拒绝时显示实际原因与“前往系统设置”。
关闭安全存储/后台任务的副作用需要在确认弹层中说明将清除的工具数据或任务。
权限页退出后释放页面状态与订阅，配置重建保留同一页；用户已确认的有限修改由宿主按工具串行
完成，不依赖页面继续存在。进入/返回动画、授权界面和后台会话不随本次所有权调整改变。

### 后台任务

只显示工具真正创建的任务：名称、状态、最近执行结果、取消。创建操作由工具内部 UI 触发；
宿主不提供伪造任务或“检查恢复状态”。`QUEUED`、`RUNNING`、`COMPLETED`、`CANCELLED` 使用
文字加语义色，结果中不展示响应 body 或敏感数据。

### 设置与 Developer Help

设置使用“外观 / 运行与权限 / 支持”三组：外观详情；后台保障与工具权限；Developer Help 与关于。
不保留重复的页面副标题或常驻绿色“自动保存”提示。当前主题在行尾显示，说明只在现有主题选择
页出现；明暗选择与系统取色分别控制，字体增大时文字自然换行。选择后即时保存并生效；连续选择
按最后一次为准。保存失败保留上次生效外观并显示重试，其他入口保持可用。关于版本来自
BuildConfig。后台总开关仍在后台保障内部页：关闭会取消所有工具后台任务及相应通知，重新打开
不自动恢复旧任务；本轮不改变此行为。

Developer Help 与仓库共用 [开发手册](sdk/help/manual.md)。采用“章节 → 主题 → 正文/代码”
两级折叠，同级只展开一项，初始全部折叠。搜索跨标题、
正文和代码匹配主题，不自动铺开长正文；清除搜索恢复目录。章节与主题整行可点，提供展开/折叠
语义与至少 48dp 命中区。只组合当前展开内容，代码自然换行、可长按选择、逐块复制或复制整份
手册；正文不使用全段高度动画。文件读取与解析离开主线程，单一 LazyColumn 使用稳定 key。
最小模板、通用打包器与完整 SDK 的嵌入代码通过独立脚本做源文件一致性检查。页面末尾保留
“安装四个范例”，不增加调试器、签名管理或安全审核器。

### 运行容器

```text
┌──────────────────────────────┐
│                              │
│                              │
│          HTML 工具内容        │
│                              │
│                              │
└──────────────────────────────┘
          系统手势区域
```

系统返回手势或按键退出工具；小工具在内容区域右滑时自行返回内部上一层，根页保持在工具内，
并保留横向控件与无障碍返回按钮。宿主不拦截所有内容滑动，不显示悬浮按钮或技术状态条。
进入过渡壳与正常运行页共用此布局；renderer 崩溃时保留简洁恢复操作。

## 6. 组件映射

| 区域 | 当前组件 | 适配责任 |
|---|---|---|
| 双主题 | `ToolBoxTheme` | 统一风格、明暗、系统取色和材质 token。 |
| 顶栏 | `ToolBoxLargeTopBar` / `ToolBoxTopBar` | Miuix 顶栏与 Liquid Glass 34sp 一级标题；inset 只消费一次。 |
| 主导航 | `ToolBoxNavigationBar` | Miuix 底栏 / Liquid Glass 悬浮胶囊，共用目的地和点击语义。 |
| 分组、身份图 | `ToolBoxGroupedSurface` / `CatalogToolGlyph` | 单一分组底板；异步显示当前安装版本的实际图标。 |
| 工具首页 | `ToolManagerScreen` / `CatalogRunningTools` | 搜索、导入、正在运行、最近图标与已安装列表，全部基于真实状态。 |
| 权限与外观 | `ToolBoxSwitchSettingRow` / `ToolBoxChoiceSettingRow` | 整行与控件可点，至少 48dp，保存失败可重试。 |
| 删除、停止、取消 | `ToolBoxDestructiveButton` | 语义危险色、禁用反馈与独立确认。 |
| Developer Help | `DeveloperHelpScreen` / `ToolBoxDisclosureRow` | 离线正文、分层折叠、搜索和代码复制。 |
| 运行页 | `ToolBoxRuntimeScaffold` | 仅处理系统安全区域，内容由硬化 WebView 承载。 |

主题入口位于 [ToolBoxTheme.kt](core-ui/src/main/java/io/toolbox/core/ui/theme/ToolBoxTheme.kt)，
布局、玻璃、行与图标适配集中在 [component](core-ui/src/main/java/io/toolbox/core/ui/component)。
HTML 小工具不使用这套宿主组件。HyperX 不是当前依赖，未来接入须固定源码版本。

一级工具/设置共用常驻 `PrimaryScreen` 与 Haze state；二级页面使用 `ToolBoxRoute` 栈和各自的
采集状态。转场、被覆盖、降低透明度或无硬件加速时停止采集；WebView 永不注册为玻璃背景源。
菜单、确认和失败反馈使用清晰 surface 上的 Miuix menu/dialog/snackbar。导航不叠加第二套
Navigation3 转场，也不让前后两张完整页面同时参与动画。

适配层负责版本差异、语义、inset 和组件默认值；业务页面不得直接依赖大量第三方 API。

## 7. 可访问性与验收

- TalkBack 顺序为标题 → 页面主操作 → 列表行打开 → 独立管理操作；管理按钮描述为“管理
  [工具名]”，行主操作提示“打开[工具名]”。
- 文字 2.0、横屏、深色、RTL 和三键/手势导航下没有裁剪、重叠或不可达控件。字体缩放达到
  1.5 倍时，主导航切换到 Miuix `IconOnly`，保留 TalkBack 标签，避免固定导航高度裁剪文字。
- 状态不只靠颜色，错误、任务状态和开关都具有文字与语义描述。
- 普通页一屏至少能见 5 个设置行或 4 个工具行；不为“高级感”扩大空白。
- 真机验收需确认：导入、删除、权限开关、运行、IME、底栏、返回手势和页面切换都可用且无
  明显卡顿。
- 120Hz 真机以 8.33ms 为单帧预算；任何页面转场修改必须分别测进入和返回，不能用一条路径的
  结果代替另一条，也不能用模拟器结果代替 WebView Surface 与系统栏的真机表现。
