# ToolBox OpenDesign 界面规范

本仓库以 OpenDesign 项目 `04260a2f-f55c-40f3-ab23-0ab14063fa02` 的最新
[HTML](design/opendesign/toolbox-ios-redesign.html) 为视觉与交互参考。
原始 HTML、brand-spec、三张标注图原样保存在 `design/opendesign/`，来源与校验值见
[source.json](design/opendesign/source.json)。HTML 的实际 CSS 优先于早期 brand-spec 中的分组列表文字。

## 唯一主题

宿主只呈现 OpenDesign Liquid Glass。Miuix 继续作为底层 Compose 控件依赖，不作为第二种用户主题。
旧 DataStore 风格字段保留可读，升级不清除颜色、透明度、后台或工具数据；任何历史风格值都呈现唯一主题。
明暗模式、系统强调色和降低透明度通过原 DataStore 状态流即时保存，无需重启、重建导航或 WebView。
设置写入失败继续显示已保存状态与可执行错误。

## 视觉

- 背景 `#F2F2F7`，文字 `#1C1C1E`，次级文字 `#5F5F64`，ToolBox 蓝 `#1264CC`。
  深色分别为 `#000000`、`#F5F5F7`、`#AEAEB2`。主按钮/危险按钮前景沿用对比度保护。
- 页面右上与左下保留低强度环境光场；原生内容卡片使用固定透光底板、1dp 边缘高光和轻阴影。
  内容卡片不建立逐行模糊采集，不随滚动触发逐行渲染特效。
- 导航保留 Haze 真实背景模糊：32dp、低噪声，浅色染色 48%；降低透明度及不可采集场景使用实色。
- 一级标题 34sp，列表标题 16sp，正文 17sp，分组/辅助 13sp。使用 Android 系统字体，不打包 SF 字体。
- 工具卡片圆角 20dp、行高至少 76dp、卡间距 8dp，图标 42dp。最近使用图标 48dp，带真实工具名。
- 底栏高 70dp、圆角 25dp；工具与设置各占一半，18dp 图标与文字横向居中排列，选中片圆角 19dp。
- 顶部不随模糊激活状态在白色和灰色之间切换。Tab 不做整页透明度、缩放或位移动画。
  导航选中使用不依赖模糊的实色浅强调色胶囊、强调色图标文字与细边框，确保玻璃和降低透明度模式均可辨识。
  导航选中/按压使用 160–170ms 动效，沿用 Compose 的系统动画缩放设置。
- 点击区至少 48dp；字体使用 sp 并自然增高换行，不按 fontScale 放大整个容器。
  宽屏继续使用可访问的原生侧导航和内容宽度限制。

## 按钮轮廓

- 独立次要操作（继续使用、取消、重试、重新选择、复制全部等）默认使用 1dp 细边框，轮廓与 Miuix 按钮使用同一 squircle 圆角，不依赖白色玻璃高光。
- 边框从当前主题的次级文字色取得：常态 78% 不透明度、按下 100%、禁用 36%，浅深色均保留可辨识的轮廓；命中区域仍至少 48dp。
- 主操作、危险操作和停止按钮保留已有色块边界。导航返回/加号保留现有玻璃圆形底座；提示关闭、搜索清除和系统原生弹窗沿用其所在容器或平台样式。
- 工具行的“管理”和代码块内的“复制”是行内操作，显式使用 `outlined = false`；新增独立次要操作应使用默认描边。

## 页面与真实交互

- 工具：导入加号、搜索、真实运行会话、带名称的最近使用、独立工具卡。主区域打开，管理按钮进入详情。
  搜索保留正在运行区，无匹配显示空结果；不伪造默认数据。工具图标仍以实际安装包为准。
- 运行会话：工具名打开已有会话，状态文字和“停止”操作明确分离，停止前确认并处理失败。
- 导入：独立页面选择系统文件，展示真实后台检查/安装结果、重选和查看工具。
  同版本/降级仍需确认，沿用原事务、清理、权限和完整性规则；HTML 的扩展名演示不作为安装校验。
- 设置：唯一界面样式、外观模式、后台保障、系统后台设置、工具权限、Developer Help、关于。
- 外观：显示当前唯一风格，跟随系统/浅色/深色直接选择，系统取色与玻璃材质各自独立成组；降低透明度只在外观页提供入口。
- 详情：真实图标与身份、打开、权限、后台任务、信息和独立危险操作。删除前确认。
- 权限、后台与帮助复用现有原生业务页面及统一组件，不复制 HTML 中的假数据与占位正文。
- 运行：HTML 占满系统安全区域，宿主不覆盖按钮、顶栏、底栏或手势；系统边缘返回或返回键先显示“返回 ToolBox？”确认，继续使用/关闭弹窗保留当前页面，确认后才退出工具。
  WebView 不参与玻璃采集或页面变形，保留原有分层导航与会话生命周期。
- 二级标题居中、圆形返回按钮；返回仍使用 Android 导航栈。状态栏与导航 inset 只消费一次。

## 实现映射

| HTML | Android |
|---|---|
| `.phone-screen` | `toolBoxOpenDesignCanvas` |
| `.group` / `.tool-row` | `toolBoxOpenDesignSurface` / `ToolBoxGroupedSurface` |
| `.search` | `ToolBoxSearchField` |
| `.bottom-nav` / `.tab` | `ToolBoxNavigationBar` |
| 内联导航 SVG | `OpenDesignIcons`，原始路径本地转换 |
| `.secondary-header` | `ToolBoxTopBar` |
| `.import-picker` / `.import-result` | `ImportScreen` + 原 `ImportViewModel` |
| `.switch` / 显示模式 | 原 Miuix 权限开关和 radio 控件 |

手机外框、假状态栏、Dynamic Island 和营销介绍只属于 HTML 展示，不复制到 Android 应用。
HTML 可直接在浏览器打开，作为原始设计对照；原生 API、安装与权限的验证使用 Android APK。
自动截图测试及 golden 门禁保持退役，人工运行截图不作为自动截图测试。

后台保障的正在运行区域复用首页任务行：真实图标、名称、状态与右侧停止按钮。3 个及以上会话时在列表底部显示全部停止；单项及批量操作都确认后执行，批量操作仅针对确认时列出的会话，逐项失败不阻断其余任务，并给出可重试反馈。

### 备份与恢复

设置入口使用现有分组行；详情页复用 ToolBox 原生组件，按说明、操作、进度、覆盖确认与结果分阶段显示。只在导出前确认一次敏感数据提示；恢复确认同时列出版本冲突、跳过内容和运行任务影响。工具内部 HTML 不参与该流程。
