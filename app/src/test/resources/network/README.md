localhost.p12 is a public test-only self-signed localhost certificate and private key, password `test-only`.
It is used solely by the controlled loopback TLS fixture. Production TLS validation is unchanged.
