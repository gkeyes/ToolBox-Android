# Design generation prompt

Built-in image generation; one selected five-state design, using the supplied Home screenshot as reference.

```javascript
// @exec: {"yield_time_ms": 120000, "max_output_tokens": 1000}
const result = await tools.image_gen__imagegen({
  referenced_image_paths:["/tmp/codex-remote-attachments/01a0a0c7-7bed-7d40-b7fe-bc8e2440a040/39525eeb-81cd-4a8f-92aa-17d0c21376f3/1-Photo-1.jpg"],
  prompt:"Use case: ui-mockup. Produce ONE polished, high fidelity design board containing FIVE equal portrait Android app surfaces arranged horizontally, at high resolution, without device frames. These are five states of ONE approved redesign of ToolBox, not alternative designs. Input image is a reference for exact existing pale blue/lavender Liquid Glass style, colorful installed tool icons, Chinese typography and bottom navigation. Remove orange annotation marks and the hardware/status black pill; don't draw phone hardware. Keep the app identity and light glass backgrounds, white restrained rounded surfaces, blue accent and large black 首页 heading. First surface: normal Home light theme. Header 首页 with a small plain text 整理 action and circular + import. Strong 18sp semibold black section headings 最近使用, 收藏, 分组. Recent tools single horizontal icon strip: 筑账·装修预算 (two line), NextFlux, RSS 探测, 2048. Favorites is a clean icon GRID of NextFlux and 2048 without row cards, descriptions or more buttons. 分组 heading has lightweight 新建分组 at right. An expanded SINGLE neutral rounded group container has black header RSS 工具 with small inline count 2 and down chevron; below in SAME container TWO icons side by side NextFlux and RSS 探测. NO individual member cards. A second COLLAPSED group has only header 日常工具 and small count 3 with right chevron, no icon preview. Bottom glass navigation 首页 / 全部工具 / 设置 unchanged. Second surface: EXACT same browser structure in coherent deep charcoal DARK theme, same layout and clear hierarchy, restrained glass, do not invent extra functions. Third surface: light Home 整理 mode. Header action 完成 instead of 整理 and import. A short helper 点击工具进行操作，长按拖动排序. Same icon grids, subtle small ellipsis badges ONLY on tool tiles in editing mode and a discrete group drag handle. No giant outline buttons and no shaking icons. Fourth surface: light Home with BOTTOM action sheet, background dimmed. Sheet identity header RSS 探测 with purple app icon and close x. Two distinct equal QUICK ACTION cells star 收藏 and folder 加入分组, no switches. A separator and a SINGLE subdued navigation row 管理工具 with chevron and small explanation 权限、信息与卸载. NO large 完成 button. Fifth surface: same bottom sheet has moved into group selection state. Header back arrow 加入分组 and close x, search 搜索分组, checkbox multi-select rows RSS 工具 selected, 日常工具 unselected; footer lightweight 新建分组. Use a top horizontal board caption design-only: 浏览·浅色, 浏览·深色, 整理, 工具操作, 分组选择. Mockups must look professionally composed with consistent 16dp page margins, 12dp grid gap, 52dp app glyphs, ~four grid columns available, 24dp section separation, labels max two lines, 48dp touch targets. Favor recognizable original green RSS NextFlux, purple document RSS 探测, dark 2048 icons from the reference. Exact Chinese labels above, visually sharp typography. This is a design target, NOT a screenshot of the running app. Do not add phone frames, unrelated illustration, oversized edit buttons, installed status labels, tool rows inside groups, colorful folder backgrounds or alternative concepts."
});
store("homeUiDesign",result);
generatedImage(result);
text({keys:Object.keys(result??{}), output_hint:result?.output_hint});

```
